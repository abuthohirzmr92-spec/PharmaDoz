# P0.9B — Excel Foundation

> MODE: IMPLEMENTATION COMPLETE  
> Excel is an INPUT SOURCE only

---

## Architecture

```
Excel (.xlsx)
  ↓
excel/parser.ts (parseExcel)
  ↓ ImportRow[]
  ↓
import/import-mapper (mapRowsToDraftItems)   [shared]
  ↓ PurchaseDraftItem[]
  ↓
import/import-pipeline (enrichImportedItems) [shared]
  ↓ enriched items
  ↓
import/import-draft-builder (buildImportDraft) [shared]
  ↓
PurchaseDraft
  ↓
Human Review
  ↓
confirmDraft()
  ↓
addPurchase() (EXISTING)
```

---

## Files

| File | Purpose | Lines |
|------|---------|-------|
| `excel/parser.ts` | parseExcel() — .xlsx → ImportRow[] | 158 |
| `excel/excel-service.ts` | importExcelToDraft() — thin orchestration | 40 |

## Shared Core (reused)

| Engine | Purpose |
|--------|---------|
| `import/import-mapper` | Row → DraftItem |
| `import/import-pipeline` | Match + Warnings |
| `import/import-draft-builder` | Totals + Status + Source |
| `import/import-status` | Status SSOT |
| `import/import-source` | Source reference |

## Safety Audit

| Check | Result |
|-------|--------|
| `matchProduct()` in excel/ | 0 |
| `generateWarnings()` in excel/ | 0 |
| `calculateDraftTotals()` in excel/ | 0 |
| Repository access | 0 |
| Store access | 0 |
| Inventory access | 0 |
| DB writes | 0 |
