# P0.9A.1 — CSV Foundation Hardening Audit

> STATUS: COMPLETE

---

## Fixes Applied

| # | Issue | Fix |
|---|-------|-----|
| 1 | Header not validated | `validateHeader()` — exact column match required |
| 2 | rowNumber off-by-one | `i + 2` (1=header, 2=first data row) |
| 3 | `crypto.randomUUID()` in mapper | Removed — `MapperDeps.generateItemId()` injected |
| 4 | Totals hardcoded to 0 | Uses `calculateDraftTotals()` from draft-engine |
| 5 | `sourceReference = null` | Set to `"csv-import"` for audit trail |
| 6 | Quoted CSV values not stripped | `trimValue()` removes surrounding quotes |

---

## Identity Audit

| Check | Result |
|-------|--------|
| `Math.random()` | 0 |
| `Date.now()` | 0 |
| `counter++` | 0 |
| `slice(0,8)` | 0 |
| `substring()` | 0 |
| Index-based keys | 0 |

---

## Boundary Audit

| Check | Result |
|-------|--------|
| `addPurchase()` calls | 0 |
| Repository access | 0 |
| Supabase access | 0 |
| Stock movement logic | 0 |
| DB writes | 0 |
| Store imports | 0 |

---

## Shared Architecture (CSV + Excel)

```
CSV / Excel
  ↓
Parser (pure)
  ↓
Mapper (pure, deps-injected)
  ↓
match-engine (pure)
  ↓
warning-engine (pure)
  ↓
draft-engine.calculateDraftTotals (pure)
  ↓
PurchaseDraft
  ↓
Human Review
  ↓
confirmDraft()
  ↓
addPurchase() (EXISTING)
```

Both CSV and Excel share: mapper deps pattern, match-engine, warning-engine, draft-engine totals.
