# CALL GRAPH — SLE Application Layer

(Generated by static analysis — Phase 2 Audit)

```
═══════════════════════════════════════════════════════════════════
OWNER PORTAL (Phase 6) — route-based under /settings/subscription
═══════════════════════════════════════════════════════════════════

UI (page.tsx / layout.tsx)
 ├─▶ useAsync hook → subscriptionRepo.getCurrent() ──▶ SELECT subscriptions
 ├─▶ useAsync hook → quotaRepo.listUsage() ──▶ SELECT tenant_quota_usage
 ├─▶ useAsync hook → invoiceRepo.listByTenant() ──▶ SELECT invoices
 ├─▶ useAsync hook → FeatureResolver.getEnabledFeatures() ──▶ SELECT package_features
 ├─▶ useAsync hook → subscriptionRepo.getTimeline() ──▶ SELECT subscription_events
 ├─▶ Quick Actions → server actions
 │    ├─▶ payInvoice() ──▶ createPrivilegedBilling() ──▶ BillingService.initiatePayment()
 │    │    └─▶ PaymentProviderManager.getActiveProvider() ──▶ PaymentProvider.createPayment()
 │    │         └─▶ ManualProvider ──▶ returns pending intent
 │    └─▶ submitUpgradeRequest() ──▶ (stub — returns ack only)
 └─▶ ViewModel helpers (pure, no I/O)
      ├─▶ deriveNextAction()
      ├─▶ statusDisplay()
      ├─▶ tenantHealthScore()
      ├─▶ resourceHealth()
      ├─▶ buildComparisonMatrix()
      └─▶ promoOutcome()

═══════════════════════════════════════════════════════════════════
PLATFORM PORTAL (Phase 7) — route-based under /platform
═══════════════════════════════════════════════════════════════════

UI (page.tsx)
 ├─▶ Dashboard
 │    ├─▶ sleKpiCards() (ViewModel, pure)
 │    ├─▶ attentionItems() (ViewModel, pure)
 │    └─▶ platformHealthHero() (ViewModel, pure)
 │
 ├─▶ Trial Desk (/platform/trials)
 │    ├─▶ trialRequestRepo.listQueue() ──▶ SELECT trial_requests
 │    └─▶ Server Actions (privileged, service-role)
 │         ├─▶ startReview() ──▶ trialRequestRepo.startReview()
 │         ├─▶ approveWithPlan() ──▶ trialRequestRepo.approve()
 │         │    ⚠️ GAP-001: provision_tenant NOT called
 │         └─▶ rejectTrial() ──▶ trialRequestRepo.reject()
 │
 ├─▶ Subscriptions (/platform/subscriptions)
 │    ├─▶ superAdminRepo.getAllTenants() ──▶ SELECT tenants
 │    ├─▶ subscriptionRepo.getCurrent(per tenant) ──▶ SELECT subscriptions
 │    └─▶ Server Actions
 │         ├─▶ suspendSubscription() ──▶ subscription_transition RPC ✅
 │         ├─▶ reactivateSubscription() ──▶ subscription_transition RPC ✅
 │         └─▶ cancelSubscription() ──▶ subscription_transition RPC ✅
 │
 └─▶ Tenant Detail (/platform/tenants/[id])
      ├─▶ (existing) superAdminRepo.changeSubscription() ──▶ UPDATE tenants + INSERT subscription_events ⚠️ bypass RPC
      ├─▶ (existing) superAdminRepo.suspendSubscription() ──▶ UPDATE subscriptions + INSERT subscription_events ⚠️ bypass RPC
      └─▶ SleTenantSection → subscriptionRepo.getCurrent() + invoiceRepo.listByTenant()

═══════════════════════════════════════════════════════════════════
SCHEDULER (Phase 4)
═══════════════════════════════════════════════════════════════════

Vercel Cron (vercel.json)
 ├─▶ /api/cron/subscription-sweep ──▶ createPrivilegedScheduler()
 │    └─▶ SchedulerService.runSubscriptionSweep()
 │         ├─▶ schedulerRunRepo.startRun() ──▶ INSERT scheduler_runs (idempotent)
 │         ├─▶ subscriptionRepo.listForSweep() ──▶ SELECT subscriptions
 │         ├─▶ decideSweepTransition() (pure ViewModel)
 │         └─▶ subscriptionLifecycleService.move() ──▶ subscriptionRepo.transition()
 │              └─▶ subscription_transition RPC (074) ✅
 │
 ├─▶ /api/cron/reminder-dispatch ──▶ createPrivilegedScheduler()
 │    └─▶ SchedulerService.runReminderDispatch()
 │         ├─▶ schedulerRunRepo.startRun() ──▶ INSERT scheduler_runs
 │         └─▶ reminderService.dispatchDue()
 │              ├─▶ reminderRepo.listDue() ──▶ SELECT reminders
 │              ├─▶ reminderRepo.logNotification() ──▶ INSERT notification_log
 │              └─▶ reminderRepo.markSent() ──▶ UPDATE reminders
 │
 └─▶ /api/cron/autorenew — ❌ GAP-006 (NOT IMPLEMENTED)

═══════════════════════════════════════════════════════════════════
WEBHOOK (Phase 5)
═══════════════════════════════════════════════════════════════════

POST /api/webhooks/payment/[provider]
 ├─▶ paymentProviderManager.getProvider(key) ──▶ (registry lookup)
 ├─▶ provider.verifyWebhook(payload, headers) ──▶ (adapter — fail-closed)
 ├─▶ provider.processWebhook(payload) ──▶ WebhookEvent (canonical)
 └─▶ createPrivilegedBilling().recordPayment(provider, event)
      ├─▶ BillingService.recordPayment()
      │    ├─▶ invoiceRepo.getById() ──▶ SELECT invoices
      │    ├─▶ subscriptionRepo.existsEventByCorrelation() ──▶ SELECT subscription_events (idempotency)
      │    ├─▶ subscriptionRepo.getCurrent() ──▶ SELECT subscriptions
      │    ├─▶ subscriptionLifecycleService.move() ──▶ subscription_transition RPC ✅
      │    ├─▶ invoiceRepo.updateStatus("paid") ──▶ UPDATE invoices
      │    ├─▶ paymentRepo.record() ──▶ INSERT payments
      │    └─▶ reminderService.notifyPaymentReceived() ──▶ INSERT notification_log
      │
      ⚠️ GAP-005: NO INSERT INTO webhook_deliveries (dedup table exists but unwired)
      ⚠️ GAP-004: NO promotion redemption recording

═══════════════════════════════════════════════════════════════════
PROVISIONING (existing, pre-SLE)
═══════════════════════════════════════════════════════════════════

provisioning.ts (server action)
 ├─▶ validateProvisioning()
 ├─▶ authClient().signUp() ──▶ creates auth.users
 └─▶ supabase.rpc("provision_tenant", {...}) ──▶ provision_tenant RPC (072)
      ├─▶ INSERT tenants (+ status)
      ├─▶ INSERT subscriptions (+ lifecycle_state, subscription_type)
      ├─▶ INSERT tenant_quotas + tenant_quota_usage (dual-write)
      ├─▶ INSERT subscription_events (trial_activated)
      └─▶ INSERT activity_logs

═══════════════════════════════════════════════════════════════════
LEGACY SUBSCRIPTION OPERATIONS (super-admin.ts)
═══════════════════════════════════════════════════════════════════

⚠️ ALL of these bypass the subscription_transition RPC (Single Writer Principle):

 superAdminRepo.changeSubscription()
  └─▶ UPDATE tenants SET package_id
  └─▶ UPDATE subscriptions SET previous_package_id
  └─▶ INSERT subscription_events (event_type: package_changed | subscription_created) ⚠️ bypass

 superAdminRepo.suspendSubscription()
  └─▶ UPDATE subscriptions SET status = 'canceled'
  └─▶ INSERT subscription_events (event_type: 'suspended') ⚠️ bypass

 superAdminRepo.reactivateSubscription()
  └─▶ UPDATE subscriptions SET status = 'active'
  └─▶ INSERT subscription_events (event_type: 'reactivated') ⚠️ bypass

 superAdminRepo.cancelSubscription()
  └─▶ UPDATE subscriptions SET status = 'canceled'
  └─▶ INSERT subscription_events (event_type: 'canceled') ⚠️ bypass

 packageRepo.logSubscriptionEvent()
  └─▶ INSERT subscription_events (event_type from caller) ⚠️ bypass
